# Local secrets/config for PlatformIO build (never commit real values).
WIFI_SSID="your-2.4ghz-ssid"
WIFI_PASSWORD="your-wifi-password"
OTA_PASSWORD="your-ota-password"

# Optional: override OTA IP used by [env:esp32_epaper_ota]
# ESP32_OTA_IP="192.168.2.2"
